<!-- <?php

    // require 'inclure_class.php';

    // $db = new PDO ("mysql:host=localhost; dbname=sb_signin", "root", "");
    // $db -> setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);

    // $manager = new users_maj_manager($db);

    // if (isset($_POST['modifier'])) {
    //     $user_maj = new user_maj (
    //         [
    //             'info' => $_POST['info'],
    //             'photo' => $_POST['photo']
    //         ]
    //     );

    //     if ($user_maj->is_user_valid()) {
    //         $manager -> insert($user_maj);
    //         header('Location: ../profil_actus/profile.php');
    //     } else {
    //         $erreurs = $user_maj -> get_erreurs();
    //         header('Location: ../profil_actus/profile.php');
    //     }
    // }

?> -->